# Demotaskpro
Frontend folder is used to design and manage the UI using React. Backend folder is used for database connectivity and query management using Node.js and Express.js. First, run npm install to download the node modules, then run npm start to start the project. Import the database to ensure proper functionality. 
